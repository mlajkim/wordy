// types
// import { CREATE_EVENT } from '../actions/eventAction';
// import { CreateEventPayload } from '../actions/eventAction';

/*
export const createEventMdl = ({dispatch, getState} : any) => (next: any) => (action: any) => {
  next(action);

  if (action.type === CREATE_EVENT) {
    // it has to be renamed as eventAction because 'action' was firstly taken by the redux  
    const { eventAction, payload }: CreateEventPayload = action.payload;


  }
};

*/

export const eventMdl = []; 